//Objeto math

let resultado;

resultado = Math.PI; 
resultado = Math.round(2.2); //redondeo simple
resultado = Math.ceil(2.2); //redondeo hacia arriba
resultado = Math.floor(2.9); //redondeo hacia abajo
resultado = Math.sqrt(144); //raiz cuadrada
resultado = Math.abs(-200); //convierte valor absoluto
resultado = Math.min(3,5,1,2,4,6); //devuelve el valor minimo de un listado
resultado = Math.max(3,5,7,2,4,6); //devuelve el valor maximo de un listado
resultado = Math.random(); //numero aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 30); //combinacion de funciones 

console.log(resultado);